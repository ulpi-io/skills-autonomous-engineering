// The editorial source of truth for the generated website.
//
// Keep this list explicit rather than inferring marketing copy from SKILL.md at
// build time. The source files remain the authority; this catalog is the
// deliberately concise, page-shaped projection of their current contracts.

export const groups = [
  {
    slug: 'lifecycle',
    index: '01',
    title: 'Lifecycle phases',
    shortTitle: 'Phases',
    description: 'Eight fail-closed phases carry a change from a grounded request to release-ready work.',
    skillSlugs: [
      'auto-spec',
      'auto-plan',
      'auto-build',
      'auto-simplify',
      'auto-test',
      'auto-review',
      'auto-performance',
      'auto-ship',
    ],
  },
  {
    slug: 'primitives',
    index: '02',
    title: 'Control primitives',
    shortTitle: 'Primitives',
    description: 'The bounded loops, skeptic panels, durable state, parallel maps, and stop conditions underneath every run.',
    skillSlugs: [
      'converge-loop',
      'adversarial-verify',
      'checkpoint-resume',
      'fan-out-work',
      'budget-guard',
    ],
  },
  {
    slug: 'autonomy',
    index: '03',
    title: 'Autonomy layer',
    shortTitle: 'Autonomy',
    description: 'Compose phases into a full run, bridge external signals, and schedule repeat-safe standing work.',
    skillSlugs: [
      'autonomous-pipeline',
      'watch-and-act',
      'schedule-recurring-agent',
    ],
  },
  {
    slug: 'context',
    index: '04',
    title: 'Context & learning',
    shortTitle: 'Context',
    description: 'Verify the project map and feed proven lessons into context the next session loads automatically.',
    skillSlugs: ['auto-map', 'auto-learn'],
  },
];

export const skills = [
  {
    slug: 'auto-spec',
    name: 'auto-spec',
    title: 'Auto Spec',
    version: 'v0.1.0',
    group: 'lifecycle',
    invocation: '/auto-spec',
    argument: '<what to build — the feature/product/change request>',
    outcome: 'Turns a raw request into a repo-grounded, testable specification with explicit non-goals.',
    useWhen: [
      'Starting a new feature, product, or significant change.',
      'A broad request needs repository recon and measurable acceptance criteria.',
      'Planning needs a written contract for what will and will not be built.',
    ],
    avoidWhen: [
      'The product decision is still too ambiguous to specify; ask the blocking questions first.',
      'You need implementation tasks rather than requirements; use auto-plan.',
      'You are ready to write code; use auto-build after an approved plan.',
    ],
    workflow: [
      'Intake the request and stop on build-changing ambiguity.',
      'Recon the repository, domain, interfaces, constraints, and existing behavior.',
      'Draft objectives, observable behavior, acceptance criteria, non-goals, assumptions, and risks.',
      'Run a completeness critic until material gaps are gone or progress stalls.',
      'Write the finalized specification under .ulpi/spec/.',
    ],
    guarantees: [
      'Every requirement is grounded in observed reality or labeled as an assumption.',
      'Every acceptance criterion names an observable, testable result.',
      'Scope includes explicit non-goals.',
      'Material ambiguity is surfaced rather than silently guessed.',
      'A stalled critic returns the open gaps instead of a false complete verdict.',
    ],
    outputs: [
      '.ulpi/spec/<name>.md',
      'Testable acceptance criteria and explicit non-goals',
      'Assumption and open-question ledger',
      'Completeness-critic convergence report',
    ],
    enforcementArtifacts: [],
    failureStates: [
      'blocked_ambiguity — a product decision changes what would be built',
      'stalled — the critic cannot close the remaining material gaps',
      'incomplete — criteria remain untestable or ungrounded',
    ],
    related: ['auto-plan', 'adversarial-verify', 'fan-out-work', 'converge-loop'],
    visual: {
      kind: 'spec-funnel',
      title: 'Evidence enters. Testable requirements leave.',
      labels: ['REQUEST', 'RECON', 'DRAFT', 'REFUTE', 'SPEC'],
      caption: 'Anything invented, ambiguous, or untestable is stopped before it contaminates the plan.',
    },
  },
  {
    slug: 'auto-plan',
    name: 'auto-plan',
    title: 'Auto Plan',
    version: 'v0.1.0',
    group: 'lifecycle',
    invocation: '/auto-plan',
    argument: '<spec path, or the feature to plan (will look for .ulpi/spec/*)>',
    outcome: 'Compiles a testable spec into a validated DAG of atomic, independently verifiable build slices.',
    useWhen: [
      'A grounded specification needs an implementable task breakdown.',
      'Several changes can run in parallel once their dependencies are explicit.',
      'The build needs exact file scopes, validation commands, and specialist routing.',
    ],
    avoidWhen: [
      'The requested behavior is still vague or untestable; return to auto-spec.',
      'You need to decide what should be built rather than how to build it.',
      'You are asking the agent to implement the plan; use auto-build.',
    ],
    workflow: [
      'Read the spec and verify its repository assumptions.',
      'Decompose it into coherent tasks with at most three scoped paths each.',
      'Wire real output dependencies and produce topological execution layers.',
      'Run the deterministic structural validator.',
      'Adversarially review coverage, paths, independence, and graph correctness until clean.',
    ],
    guarantees: [
      'Every task maps to real paths or paths created by an earlier dependency.',
      'Tasks are atomic, independently verifiable, and carry slice-scoped validation.',
      'The graph is acyclic and execution layers are topologically valid.',
      'Write overlap is ordered rather than raced.',
      'A stalled review reports remaining defects and never signs off the graph.',
    ],
    outputs: [
      '.ulpi/plans/<name>.json',
      'Dependency edges and topological execution layers',
      'Acceptance-criterion-to-task coverage map',
      'Specialist assignments and self-review result',
    ],
    enforcementArtifacts: [
      {
        type: 'validator',
        path: 'auto-plan/scripts/validate-plan.mjs',
        label: 'Checks task shape, paths, dependencies, layers, and write-scope safety.',
      },
    ],
    failureStates: [
      'invalid — the deterministic graph validator rejects the plan',
      'stalled — adversarial review leaves unresolved plan defects',
      'ungrounded — a task depends on phantom paths, commands, or requirements',
    ],
    related: ['auto-spec', 'auto-build', 'adversarial-verify', 'fan-out-work'],
    visual: {
      kind: 'dag-blueprint',
      title: 'Nothing builds on a missing base.',
      labels: ['SPEC', 'ATOMIC TASKS', 'DEPENDENCIES', 'LAYERS', 'VALIDATE'],
      caption: 'The graph is executable only when its paths are real, its edges mean needs-output, and its layers are valid.',
    },
  },
  {
    slug: 'auto-build',
    name: 'auto-build',
    title: 'Auto Build',
    version: 'v0.1.1',
    group: 'lifecycle',
    invocation: '/auto-build',
    argument: "<plan path, or the feature to build (will look for .ulpi/plans/*); 'resume' to continue>",
    outcome: 'Walks an approved task DAG test-first, isolating parallel work and preserving one rollback point per task.',
    useWhen: [
      'A validated spec and DAG plan are ready for implementation.',
      'You want one approved autonomous pass with per-task commits.',
      'A long build must survive interruption and resume without redoing integrated work.',
    ],
    avoidWhen: [
      'There is no valid plan; run auto-plan first.',
      'The working tree contains unrelated changes that would compromise rollback.',
      'You are on a protected branch without explicit confirmation.',
    ],
    workflow: [
      'Validate the plan, baseline, branch, budget, and checkpoint.',
      'Record one unambiguous human approval of the plan.',
      'Run each dependency-ready layer in isolated worktrees.',
      'For each task: RED, GREEN, integrate, adversarial review, bounded fix, commit.',
      'Run the final whole-workspace validation and reconcile the checkpoint.',
    ],
    guarantees: [
      'A task starts only after all dependencies are integrated.',
      'Parallel writers are isolated and integration is serialized.',
      'Every completed slice earns a red-then-green test and its own commit.',
      'Bulk staging, destructive cleanup, and unsafe force operations are blocked by the bundled guard.',
      'Ambiguous, irreversible, or unfixable work stops for the user.',
      'Resume skips every task already marked done.',
    ],
    outputs: [
      'One scoped commit per completed task',
      'Tests and slice-validation evidence per task',
      'Final whole-workspace validation result',
      '.ulpi/runs/<id>.json checkpoint with done, blocked, and dependency-blocked tasks',
    ],
    enforcementArtifacts: [
      {
        type: 'guard',
        path: 'auto-build/scripts/guard-git-hygiene.sh',
        label: 'Blocks bulk staging, commit-all, unsafe force-push, hard reset, and destructive clean patterns.',
      },
      {
        type: 'contract',
        path: 'auto-build/references/build-contract.md',
        label: 'Defines isolated task execution, integration, review, and checkpoint behavior.',
      },
    ],
    failureStates: [
      'blocked — a slice cannot reach its validation bar',
      'dep_blocked — a prerequisite task never integrated',
      'needs_approval — an ambiguous or irreversible action exceeds the approved plan',
      'validation_red — the integrated workspace gate failed',
    ],
    related: ['auto-plan', 'converge-loop', 'fan-out-work', 'checkpoint-resume', 'budget-guard'],
    visual: {
      kind: 'worktree-rail',
      title: 'Parallel branches. Serialized truth.',
      labels: ['DAG GATE', 'WORKTREES', 'RED → GREEN', 'MERGE', 'COMMIT'],
      caption: 'Each dependency-ready slice runs alone, lands in order, and leaves a clean rollback point.',
    },
  },
  {
    slug: 'auto-simplify',
    name: 'auto-simplify',
    title: 'Auto Simplify',
    version: 'v0.1.0',
    group: 'lifecycle',
    invocation: '/auto-simplify',
    argument: '[scope — a diff, path, or module] (default: the current diff)',
    outcome: 'Reduces reader cost one small edit at a time while proving observable behavior did not change.',
    useWhen: [
      'Working code contains duplication, dead code, needless branching, or over-abstraction.',
      'A recent diff is correct but harder to understand than necessary.',
      'You want a behavior-preserving cleanup with explicit proof.',
    ],
    avoidWhen: [
      'The intended change alters behavior, fixes a defect, or adds a feature.',
      'The goal is runtime performance; use auto-performance.',
      'The purpose of the code is unknown and cannot be established.',
    ],
    workflow: [
      'Set the scope, capture the diff, and establish a green behavior baseline.',
      'Rank concrete simplification opportunities by reader cost and risk.',
      'Make one smallest clarifying edit.',
      'Run relevant tests and an adversarial semantic comparison; revert on doubt.',
      'Repeat until a full round is dry, then report what stayed and what was kept on purpose.',
    ],
    guarantees: [
      'Every retained edit passes tests and an adversarial behavior-preservation check.',
      'Code whose purpose is not understood remains behind Chesterton’s Fence.',
      'Edits remain small enough to attribute and revert independently.',
      'Clarity wins over line count or cleverness.',
      'A non-dry or stalled run is reported honestly.',
    ],
    outputs: [
      'Behavior-preserving simplification diff',
      'Ledger of kept, investigated, and reverted opportunities',
      'Final relevant-suite result',
    ],
    enforcementArtifacts: [],
    failureStates: [
      'reverted — a candidate changes behavior or loses the proof signal',
      'fenced — the code purpose cannot be established safely',
      'stalled — worthwhile opportunities remain but no verified progress is possible',
    ],
    related: ['auto-build', 'auto-test', 'adversarial-verify', 'converge-loop'],
    visual: {
      kind: 'dry-loop',
      title: 'Less code surface. Same behavior.',
      labels: ['FIND', 'EDIT ONE', 'TEST', 'REFUTE DELTA', 'DRY'],
      caption: 'A cleanup survives only when the behavior proof survives with it.',
    },
  },
  {
    slug: 'auto-test',
    name: 'auto-test',
    title: 'Auto Test',
    version: 'v0.1.1',
    group: 'lifecycle',
    invocation: '/auto-test',
    argument: "[scope — path/module/diff, or 'flaky' to stabilize] (default: the current diff)",
    outcome: 'Builds a green, meaningful suite whose new tests are proven capable of detecting broken behavior.',
    useWhen: [
      'Implemented behavior needs coverage and a trustworthy green signal.',
      'A risky module or known bug needs regression tests.',
      'A flaky suite needs root-cause stabilization rather than retries or bigger timeouts.',
    ],
    avoidWhen: [
      'You are implementing new behavior test-first; that belongs inside auto-build.',
      'The repository has no test runner configured.',
      'Expected behavior is a product decision that has not been resolved.',
    ],
    workflow: [
      'Identify the real test command, baseline signal, scope, and run budget.',
      'Discover untested behaviors rather than chasing a percentage.',
      'Write one behavior test, break the implementation, and require the test to fail.',
      'Restore the implementation and converge the suite to green.',
      'Diagnose flakes at their root and report all remaining failures honestly.',
    ],
    guarantees: [
      'Every added test is mutation-checked before it counts.',
      'The skill does not delete, skip, isolate, suppress, or weaken tests to manufacture green.',
      'A test exposing a real product defect is surfaced rather than rewritten around the defect.',
      'A capped red run reports converged:false with the exact failing units.',
      'The checkpoint makes broad coverage work resumable.',
    ],
    outputs: [
      'Behavior-focused, mutation-verified tests',
      'Baseline-to-final suite and coverage signal',
      'Real-defect and flake-root-cause ledger',
      '.ulpi/runs/<id>.json checkpoint',
    ],
    enforcementArtifacts: [
      {
        type: 'guard',
        path: 'auto-test/scripts/guard-test-integrity.sh',
        label: 'Blocks common skip, only, and suppression patterns in test edits.',
      },
      {
        type: 'quality-bar',
        path: 'auto-test/references/test-quality-bar.md',
        label: 'Defines behavioral coverage, mutation checks, and acceptable flake fixes.',
      },
    ],
    failureStates: [
      'converged:false — the suite remains red when the cap or budget fires',
      'tautology_rejected — the test stayed green against a broken implementation',
      'bug_surfaced — the test is sound and the implementation is wrong',
      'blocked_ambiguity — correct expected behavior needs a human decision',
    ],
    related: ['auto-build', 'converge-loop', 'checkpoint-resume', 'budget-guard'],
    visual: {
      kind: 'mutation-circuit',
      title: 'A test counts only after it catches the break.',
      labels: ['WRITE', 'MUTATE', 'MUST FAIL', 'RESTORE', 'MUST PASS'],
      caption: 'Green is meaningful only when the same test turns red against a deliberately broken implementation.',
    },
  },
  {
    slug: 'auto-review',
    name: 'auto-review',
    title: 'Auto Review',
    version: 'v0.1.0',
    group: 'lifecycle',
    invocation: '/auto-review',
    argument: '[scope — a diff/branch/PR, or path] (default: the current branch diff)',
    outcome: 'Fans a change across independent review lenses and reports only findings that survive adversarial refutation.',
    useWhen: [
      'A branch, pull request, diff, or module needs broad review before merge.',
      'False positives need to be filtered before they consume engineering attention.',
      'Confirmed blockers should optionally enter a bounded, scoped fix loop.',
    ],
    avoidWhen: [
      'You need the feature implemented rather than reviewed.',
      'You want behavior-neutral cleanup; use auto-simplify.',
      'You have not explicitly asked for fixes and expect a read-only review.',
    ],
    workflow: [
      'Pin the scope, baseline, diff, and review budget.',
      'Fan out correctness, security, performance, maintainability, testing, and contract lenses.',
      'Deduplicate raw findings without erasing distinct failure scenarios.',
      'Put every finding through an independent majority-refute panel.',
      'Report survivors, open quorum gaps, and rejections; fix only when requested.',
    ],
    guarantees: [
      'Only adversarially verified findings count or drive edits.',
      'A review dimension that did not run is an explicit coverage gap.',
      'Severity is calibrated to the actual failure scenario.',
      'Fixes are opt-in, minimal, scoped, and re-verified.',
      'A clean verdict is impossible while a dimension or verification quorum is missing.',
    ],
    outputs: [
      'Verified findings grouped by Blocker, Concern, Nit, and FYI',
      'Coverage report for every review dimension',
      'Unverified quorum-gap ledger',
      'Refuted-finding rejection ledger',
      'Optional bounded fixes for confirmed issues',
    ],
    enforcementArtifacts: [
      {
        type: 'workflow',
        path: 'auto-review/references/review-workflow.js',
        label: 'Runnable deterministic fan-out, deduplication, quorum, and findings-register template.',
      },
    ],
    failureStates: [
      'coverage_gap — one or more review dimensions did not return evidence',
      'unverified — a finding panel died below quorum',
      'blocked — verified blocker findings remain open',
      'fix_exhausted — an opted-in repair loop did not converge',
    ],
    related: ['adversarial-verify', 'fan-out-work', 'converge-loop', 'checkpoint-resume'],
    visual: {
      kind: 'skeptic-matrix',
      title: 'Six lenses in. Verified signal out.',
      labels: ['CORRECT', 'SECURE', 'FAST', 'CLEAR', 'TESTED', 'COMPATIBLE'],
      caption: 'Raw findings cross a second, adversarial gate before any of them reach the user.',
    },
  },
  {
    slug: 'auto-performance',
    name: 'auto-performance',
    title: 'Auto Performance',
    version: 'v0.1.0',
    group: 'lifecycle',
    invocation: '/auto-performance',
    argument: '[target — endpoint/function/page/metric, or a perf budget] (default: profile to find it)',
    outcome: 'Keeps only measured performance wins that exceed variance without changing correctness.',
    useWhen: [
      'A measurable performance target or suspected regression exists.',
      'Profiling should identify the real hotspot before optimization.',
      'Candidate optimizations need apples-to-apples benchmark evidence.',
    ],
    avoidWhen: [
      'No reproducible metric or benchmark can be established.',
      'The problem is functional correctness or a new feature.',
      'The goal is readability rather than runtime performance.',
    ],
    workflow: [
      'Define the metric, target, benchmark command, and variance-aware baseline.',
      'Profile representative work to locate the real hotspot.',
      'Change one hotspot and rerun the same benchmark.',
      'Keep the change only when the gain is real and correctness remains green.',
      'Stop at the target or diminishing returns and report every reverted attempt.',
    ],
    guarantees: [
      'No optimization starts without a reproducible baseline.',
      'Profiling evidence chooses the target instead of intuition.',
      'Acceptance requires a gain beyond run-to-run variance.',
      'Tests and adversarial regression checks protect correctness.',
      'Unmeasured or noisy claims are reverted, never rounded into wins.',
    ],
    outputs: [
      'Metric and variance-aware before/after benchmark',
      'Kept optimizations with per-hotspot measured gains',
      'Tried-and-reverted optimization ledger',
      'Target-met or stalled verdict',
    ],
    enforcementArtifacts: [],
    failureStates: [
      'no_baseline — the benchmark cannot be made reproducible',
      'reverted_no_gain — the delta is inside measurement noise',
      'reverted_regression — correctness changed',
      'diminishing_returns — remaining gain is not worth added complexity',
    ],
    related: ['auto-test', 'adversarial-verify', 'converge-loop', 'budget-guard'],
    visual: {
      kind: 'benchmark-gauge',
      title: 'The benchmark is the acceptance gate.',
      labels: ['BASELINE', 'PROFILE', 'CHANGE', 'RE-MEASURE', 'KEEP / REVERT'],
      caption: 'A faster-looking diff is still rejected until the same workload proves a real gain.',
    },
  },
  {
    slug: 'auto-ship',
    name: 'auto-ship',
    title: 'Auto Ship',
    version: 'v0.1.1',
    group: 'lifecycle',
    invocation: '/auto-ship',
    argument: '[what to ship — a branch/feature] (default: the current branch)',
    outcome: 'Turns verified work into release-ready artifacts while keeping failed, skipped, and irreversible gates honest.',
    useWhen: [
      'A built, tested, and reviewed change is ready for release preparation.',
      'The branch needs grounded notes, versioning, documentation, and a pull request.',
      'A risky rollout needs flags, monitoring, and a rollback path before launch.',
    ],
    avoidWhen: [
      'Implementation or review is still incomplete.',
      'A configured release gate is red or did not run.',
      'You expect an irreversible deploy or publication without fresh human approval.',
    ],
    workflow: [
      'Pin the release scope and adversarially gate the ready claim.',
      'Run final validation, review, security, observability, and rollback checks.',
      'Prepare atomic commits, grounded release notes, version, and user-facing docs.',
      'Open the pull request or stage a flagged rollout.',
      'Stop for explicit approval before any irreversible trigger.',
    ],
    guarantees: [
      'Every configured gate must actually run and pass.',
      'The final validation runs on the integrated release state.',
      'Release notes describe only observed changes.',
      'Risky rollout paths include monitoring and rollback before launch.',
      'Unsafe force-push and ref-deletion patterns are blocked by the bundled guard.',
      'Deploy, publication, and destructive migration triggers remain human-owned.',
    ],
    outputs: [
      'Truthful pre-launch gate matrix',
      'Grounded version, changelog, release notes, and documentation',
      'Pull request or staged-rollout plan',
      'Rollback path and explicit-approval ledger',
    ],
    enforcementArtifacts: [
      {
        type: 'guard',
        path: 'auto-ship/scripts/guard-ship-irreversibles.sh',
        label: 'Blocks force-push, mirror, prune, and remote ref-deletion spellings.',
      },
    ],
    failureStates: [
      'blocked_gate — a configured gate failed or did not run',
      'awaiting_approval — an irreversible release action is ready but not authorized',
      'no_rollback — the proposed rollout lacks a safe back-out path',
      'validation_red — the integrated release state is not green',
    ],
    related: ['auto-review', 'auto-test', 'adversarial-verify', 'checkpoint-resume'],
    visual: {
      kind: 'release-airlock',
      title: 'Unrun is not green.',
      labels: ['VALIDATE', 'REVIEW', 'SECURITY', 'ROLLBACK', 'HUMAN SIGN-OFF'],
      caption: 'Release artifacts move automatically; the irreversible trigger stays behind the final airlock.',
    },
  },
  {
    slug: 'converge-loop',
    name: 'converge-loop',
    title: 'Converge Loop',
    version: 'v0.1.0',
    group: 'primitives',
    invocation: '/converge-loop',
    argument: "<goal — e.g. 'make `pnpm test` pass' or 'remove all dead exports'>",
    outcome: 'Runs a measurable until-green or until-dry loop with explicit caps, anti-thrash, and honest termination.',
    useWhen: [
      'A task repeats until a machine-checkable condition holds.',
      'Tests, lint, audits, migrations, or coverage need bounded iteration.',
      'Each cycle can make and verify one small, attributable change.',
    ],
    avoidWhen: [
      'The task is one command or one edit.',
      'The stopping condition is subjective or cannot be measured.',
      'The work is open-ended exploration without an exit condition.',
    ],
    workflow: [
      'Choose until-green or until-dry and pin the termination set.',
      'Baseline the machine-readable signal.',
      'Diagnose one unit, apply one minimal change, and rerun the signal.',
      'Track progress and stop repeated failure signatures before they thrash.',
      'Exit on convergence, exhaustion, stall, or escalation and enumerate the remainder.',
    ],
    guarantees: [
      'Every loop declares a done condition, hard cap, budget, and no-progress rule.',
      'The done condition is machine-checkable.',
      'Each iteration is smaller than the whole problem.',
      'Caps and stalls return converged:false rather than fabricated success.',
      'User-owned decisions stop the loop instead of being guessed.',
    ],
    outputs: [
      'Declared termination set',
      'Starting-to-ending signal delta',
      'Iteration, budget, and exit-reason ledger',
      'Enumerated remaining units when not converged',
    ],
    enforcementArtifacts: [
      {
        type: 'patterns',
        path: 'converge-loop/references/loop-patterns.md',
        label: 'Reusable until-green and until-dry loop shapes.',
      },
      {
        type: 'routing',
        path: 'converge-loop/references/native-goal-loop.md',
        label: 'Maps the termination contract onto native goal and loop surfaces.',
      },
      {
        type: 'contract',
        path: 'converge-loop/references/termination-and-thrash.md',
        label: 'Defines caps, no-progress signatures, and anti-thrash exits.',
      },
    ],
    failureStates: [
      'exhausted — the hard cap or budget fired before the done condition',
      'stalled — maxStall no-progress rounds were reached',
      'escalated — progress requires an ambiguous, external, or destructive decision',
    ],
    related: ['budget-guard', 'checkpoint-resume', 'adversarial-verify'],
    visual: {
      kind: 'termination-orbit',
      title: 'Every orbit has four exits.',
      labels: ['MEASURE', 'DIAGNOSE', 'CHANGE ONE', 'RE-MEASURE', 'STOP'],
      caption: 'Done, capped, stalled, or escalated: there is no hidden fifth state called keep grinding.',
    },
  },
  {
    slug: 'adversarial-verify',
    name: 'adversarial-verify',
    title: 'Adversarial Verify',
    version: 'v0.1.0',
    group: 'primitives',
    invocation: '/adversarial-verify',
    argument: '<the claim/finding to verify, or a path to a findings list>',
    outcome: 'Makes independent skeptics try to disprove a claim and keeps it only when it survives a fail-closed vote.',
    useWhen: [
      'An agent assertion could drive an expensive or hard-to-reverse action.',
      'A defect, fix, benchmark win, or clean verdict needs proof from the underlying evidence.',
      'Several failure lenses need independent scrutiny.',
    ],
    avoidWhen: [
      'A fact is already machine-proven by a successful command or test.',
      'The verifiers cannot access the actual code, diff, reproduction, or invariant.',
      'The originating claimant would be its own verifier.',
    ],
    workflow: [
      'Rewrite the claim as a concrete, falsifiable proposition.',
      'Choose an odd panel sized to the stakes and assign distinct failure lenses.',
      'Give independent skeptics the ground truth and tell each to refute.',
      'Tally the evidence with majority-survive and fail-closed quorum rules.',
      'Return survivors and the complete rejection ledger.',
    ],
    guarantees: [
      'Verifier prompts are adversarial, not confirmation-seeking.',
      'The claimant never sits on its own panel.',
      'Every verifier receives ground truth rather than a summary alone.',
      'Ties, abstentions, dead verifiers, and insufficient evidence do not pass.',
      'Concrete counterevidence can outweigh bare majority assertions.',
    ],
    outputs: [
      'Panel size, lenses, and evidence per claim',
      'Survived-claim list',
      'Rejected-claim and counterexample ledger',
      'Fail-closed clean or not-clean verdict',
    ],
    enforcementArtifacts: [
      {
        type: 'patterns',
        path: 'adversarial-verify/references/verify-patterns.md',
        label: 'Panel prompts, lens patterns, bulk gates, and evidence-over-headcount rules.',
      },
    ],
    failureStates: [
      'rejected — a majority refuted the claim or could not establish survival',
      'no_quorum — verifiers died, timed out, or returned empty evidence',
      'ambiguous — evidence cannot support a safe survived verdict',
    ],
    related: ['fan-out-work', 'converge-loop', 'auto-review'],
    visual: {
      kind: 'vote-ledger',
      title: 'The claim starts guilty.',
      labels: ['CLAIM', 'REFUTER 1', 'REFUTER 2', 'REFUTER 3', 'SURVIVE / REJECT'],
      caption: 'Independent skeptics receive the evidence and a mandate to break the proposition.',
    },
  },
  {
    slug: 'checkpoint-resume',
    name: 'checkpoint-resume',
    title: 'Checkpoint & Resume',
    version: 'v0.1.1',
    group: 'primitives',
    invocation: '/checkpoint-resume',
    argument: '<run label, or a path/id to resume>',
    outcome: 'Persists long-run state to disk so a fresh session skips verified work already completed.',
    useWhen: [
      'A run has many independently identifiable units.',
      'Interruption, crash, or session loss should not restart completed work.',
      'Operators need a read-only view of live and terminal run state.',
    ],
    avoidWhen: [
      'The task is a single indivisible action.',
      'Units cannot be identified or verified independently.',
      'You intend to use checkpoint writes as a delivery gate.',
    ],
    workflow: [
      'Decide new run or resume before touching state.',
      'Initialize through the locked checkpoint CLI; never overwrite a live run.',
      'Persist truthful phase and unit transitions as work lands.',
      'On resume, load disk state and rebuild only non-done eligible units.',
      'Finalize honestly and expose status through the read-only viewer.',
    ],
    guarantees: [
      'Disk state, not an in-memory cache, is the source of truth.',
      'Resume skips verified done units and respects dependency readiness.',
      'Locked atomic writes protect concurrent updates.',
      'The CLI refuses live reinitialization, done-state demotion, and false successful finalization.',
      'Status-write failure remains non-fatal observability and does not falsely fail delivery.',
    ],
    outputs: [
      '.ulpi/runs/<id>.json durable state',
      'Per-phase and per-unit timestamps and status',
      'Open-item and needs-attention register',
      'Read-only rendered status and resume command',
    ],
    enforcementArtifacts: [
      {
        type: 'cli',
        path: 'checkpoint-resume/scripts/checkpoint.mjs',
        label: 'Locked, atomic state transitions with fail-closed lifecycle refusals.',
      },
      {
        type: 'viewer',
        path: 'checkpoint-resume/scripts/run-status.mjs',
        label: 'Read-only newest, list, JSON, and resume views.',
      },
      {
        type: 'schema',
        path: 'checkpoint-resume/references/status-schema.md',
        label: 'Documents durable run, phase, unit, timestamp, and open-item fields.',
      },
    ],
    failureStates: [
      'needs_attention — work stopped with named open units',
      'aborted — the run terminated without meeting its completion bar',
      'stale_observability — status writes failed and state must be reconstructed from real artifacts',
      'refused_transition — the CLI rejected an unsafe lifecycle mutation',
    ],
    related: ['converge-loop', 'fan-out-work', 'auto-build', 'autonomous-pipeline'],
    visual: {
      kind: 'state-tape',
      title: 'Crash here. Resume there.',
      labels: ['PENDING', 'RUNNING', 'DONE', 'INTERRUPT', 'SKIP DONE'],
      caption: 'Verified completion survives the session because the ledger lives on disk.',
    },
  },
  {
    slug: 'fan-out-work',
    name: 'fan-out-work',
    title: 'Fan-out Work',
    version: 'v0.1.0',
    group: 'primitives',
    invocation: '/fan-out-work',
    argument: "<the work — e.g. 'add tests to every untested module' or a path to a work-list>",
    outcome: 'Runs the same bounded pipeline over independent items in parallel without hiding failures or dropped coverage.',
    useWhen: [
      'A large, cheaply discoverable list of independent items needs the same treatment.',
      'Per-file review, tests, migration, or auditing would waste wall-clock when run serially.',
      'The result can be faithfully reduced after every item reaches a terminal state.',
    ],
    avoidWhen: [
      'The list contains only a handful of items.',
      'Items depend on one another and therefore require a DAG.',
      'Parallel items share write scope without worktree isolation.',
    ],
    workflow: [
      'Scout and freeze the complete work list before delegation.',
      'Define the identical per-item pipeline and terminal outcomes.',
      'Set concurrency, retries, isolation, budget, and an explicit drop policy.',
      'Run items concurrently while the coordinator retains stop/continue judgment.',
      'Reduce results and account for every covered, failed, blocked, and dropped item.',
    ],
    guarantees: [
      'The coordinator proves independence before parallel execution.',
      'Failed or null items remain visible in the result.',
      'Any top-N, sample, or overflow cap lists exactly what it omitted.',
      'Parallel writers use isolation or are sequenced.',
      'The final accounting includes every discovered item.',
    ],
    outputs: [
      'Frozen item inventory and independence basis',
      'Per-item pipeline, isolation, retry, and drop policy',
      'Covered, failed, blocked, and dropped-item ledger',
      'Optional reduced or merged result',
    ],
    enforcementArtifacts: [
      {
        type: 'patterns',
        path: 'fan-out-work/references/fanout-patterns.md',
        label: 'Map, staged pipeline, barrier, isolation, and faithful-reduction templates.',
      },
    ],
    failureStates: [
      'failed_item — an agent returned an error or null result',
      'blocked_item — the item cannot complete inside the declared pipeline',
      'dropped_item — a named sampling or cap rule excluded the item',
      'not_independent — cross-item dependencies require a DAG instead',
    ],
    related: ['converge-loop', 'adversarial-verify', 'checkpoint-resume', 'auto-plan'],
    visual: {
      kind: 'map-reduce-grid',
      title: 'Parallel does not mean partial.',
      labels: ['SCOUT', 'MAP × N', 'ISOLATE', 'BARRIER', 'ACCOUNT'],
      caption: 'Every discovered item finishes as covered, failed, blocked, or explicitly dropped.',
    },
  },
  {
    slug: 'budget-guard',
    name: 'budget-guard',
    title: 'Budget Guard',
    version: 'v0.1.0',
    group: 'primitives',
    invocation: '/budget-guard',
    argument: '[the run to bound — a phase, a loop, or the whole pipeline]',
    outcome: 'Declares and enforces the five stop conditions that keep unattended work from becoming runaway work.',
    useWhen: [
      'Any unattended loop, fan-out, phase, or full pipeline is about to start.',
      'A run will spawn multiple agents or execute several repair rounds.',
      'The user needs a hard ceiling and a named escalation contract.',
    ],
    avoidWhen: [
      'The run has no measurable done condition.',
      'No one can state a cap, budget, no-progress rule, and escalation triggers.',
      'The intent is to raise limits silently whenever the run approaches them.',
    ],
    workflow: [
      'Define the done condition.',
      'Set iteration, agent, or item caps.',
      'Set token, tool-call, and/or wall-clock ceilings.',
      'Name no-progress signatures and escalation triggers.',
      'Monitor consumption and stop the instant any condition fires.',
    ],
    guarantees: [
      'No unattended run begins without all five stop conditions.',
      'A budget is a ceiling, not a suggestion.',
      'A capped unfinished run returns an honest partial result.',
      'Ambiguity, destructive action, repeated no-progress, and imminent exhaustion escalate.',
      'Only the user can authorize a larger budget or changed scope.',
    ],
    outputs: [
      'Five-condition stop declaration',
      'Actual iterations, agents, tokens, and time versus budget',
      'Done, capped, over-budget, or escalated exit reason',
      'Honest remaining-gap and user-decision report',
    ],
    enforcementArtifacts: [],
    failureStates: [
      'capped — a hard count limit fired before completion',
      'over_budget — the token, tool-call, or time ceiling fired',
      'stalled — the declared no-progress condition fired',
      'escalated — a named user-owned trigger fired',
    ],
    related: ['converge-loop', 'fan-out-work', 'autonomous-pipeline'],
    visual: {
      kind: 'stop-pentagon',
      title: 'Five conditions. Zero runaway exits.',
      labels: ['DONE', 'HARD CAP', 'BUDGET', 'NO PROGRESS', 'ESCALATE'],
      caption: 'The run starts only after every edge of the stop contract has a concrete value.',
    },
  },
  {
    slug: 'autonomous-pipeline',
    name: 'autonomous-pipeline',
    title: 'Autonomous Pipeline',
    version: 'v0.1.1',
    group: 'autonomy',
    invocation: '/autonomous-pipeline',
    argument: "<feature request to take from idea to shipped> (or 'resume' to continue a run)",
    outcome: 'Runs the complete engineering lifecycle once, behind one plan approval, with durable phase gates and a verified findings register.',
    useWhen: [
      'A substantial request should move from idea through ship preparation in one autonomous run.',
      'The user wants one plan approval rather than manually invoking every phase.',
      'A long multi-phase run needs exact resume and honest downstream accounting.',
    ],
    avoidWhen: [
      'Only one phase or a quick edit is needed.',
      'The user has not explicitly requested a broad autonomous run.',
      'You intend to recursively fix the pipeline’s own final findings without a new user decision.',
    ],
    workflow: [
      'Capture the request, phase selection, branch, budget, mode, and checkpoint.',
      'Define and plan, then pause for the single plan approval.',
      'Run build, simplify, test, review, performance, ship preparation, learn, and map once.',
      'Carry blocked phase evidence forward without fabricating a green handoff.',
      'Verify the open register and return next-move choices instead of auto-recursing.',
    ],
    guarantees: [
      'Plan approval is the only blanket approval; surprises and irreversibles still stop.',
      'The lifecycle makes one forward pass and never recursively fixes itself.',
      'Every phase gate records met, blocked, or skipped.',
      'A blocked phase cannot be represented as done to keep downstream work moving.',
      'Pipeline- and phase-level budgets bound the whole run.',
      'A durable checkpoint resumes at the exact unfinished phase and unit.',
    ],
    outputs: [
      'Per-phase gate and specialist-routing register',
      'Integrated validation result and ship-preparation artifacts',
      'Verified open-findings register with user-owned next moves',
      '.ulpi/runs/<id>.json pipeline checkpoint and status command',
    ],
    enforcementArtifacts: [
      {
        type: 'workflow',
        path: 'autonomous-pipeline/references/pipeline-workflow.js',
        label: 'Runnable deterministic phase orchestration, retries, checkpoint, and findings-register template.',
      },
      {
        type: 'schema',
        path: 'autonomous-pipeline/references/pipeline-state.md',
        label: 'Defines phase state, gates, config, findings, and resume semantics.',
      },
    ],
    failureStates: [
      'blocked — a phase missed its gate and remains resumable',
      'skipped — the configured run intentionally omitted a phase',
      'needs_attention — findings or irreversible work require the user',
      'budget_exhausted — the whole-run ceiling fired before completion',
    ],
    related: ['auto-spec', 'auto-plan', 'auto-build', 'checkpoint-resume', 'auto-learn', 'auto-map'],
    visual: {
      kind: 'lifecycle-ring',
      title: 'One approved pass. Eight honest gates.',
      labels: ['SPEC', 'PLAN', 'BUILD', 'CLEAN', 'TEST', 'REVIEW', 'PERF', 'SHIP'],
      caption: 'Learn and map close the run, while every blocked gate remains visible in the final register.',
    },
  },
  {
    slug: 'watch-and-act',
    name: 'watch-and-act',
    title: 'Watch & Act',
    version: 'v0.1.0',
    group: 'autonomy',
    invocation: '/watch-and-act',
    argument: "<signal to watch — e.g. 'CI on branch X' or 'deploy status'> [until <condition/deadline>]",
    outcome: 'Bridges a run to external state with bounded, cache-aware polling and explicit transition handling.',
    useWhen: [
      'CI, a deployment, queue, endpoint, or external approval changes outside the harness.',
      'The signal is unresolved and worth checking more than once.',
      'A known transition should trigger proceed, diagnose, escalate, or timeout behavior.',
    ],
    avoidWhen: [
      'The harness already tracks the background agent, task, or workflow and will wake you.',
      'One immediate status check is enough.',
      'No absolute deadline or maximum poll count can be set.',
    ],
    workflow: [
      'Confirm the signal is external, unresolved, and not harness-tracked.',
      'Set an absolute deadline, poll cap, and cache-aware cadence.',
      'Persist the bound and count before the first scheduled wakeup.',
      'Read the real state, classify it, and act on the observed transition.',
      'Stop on target, terminal failure, or timeout and report the final state.',
    ],
    guarantees: [
      'Harness-tracked background work is never polled.',
      'Every watch has a durable deadline or maximum poll count.',
      'Active cadence stays at or below 270 seconds; idle cadence uses 1200 seconds or more, avoiding roughly 300 seconds.',
      'The watcher acts on observed state rather than probability.',
      'Terminal failures escalate instead of being polled indefinitely.',
    ],
    outputs: [
      'External-signal and transition definition',
      'Cadence, deadline, and poll-count record',
      'Observed transition and action taken',
      'Final state and total wait report',
    ],
    enforcementArtifacts: [],
    failureStates: [
      'timed_out — the deadline or poll cap fired before the target',
      'terminal_failure — the signal reached a red state that will not self-resolve',
      'wrong_surface — the requested work is harness-tracked or only needs one check',
    ],
    related: ['budget-guard', 'converge-loop', 'schedule-recurring-agent'],
    visual: {
      kind: 'signal-trace',
      title: 'Observe the transition, not the clock.',
      labels: ['CHECK', 'PENDING', 'WAKE', 'GREEN / RED', 'ACT / STOP'],
      caption: 'A durable deadline surrounds every poll, and terminal red never becomes another blind retry.',
    },
  },
  {
    slug: 'schedule-recurring-agent',
    name: 'schedule-recurring-agent',
    title: 'Schedule Recurring Agent',
    version: 'v0.1.0',
    group: 'autonomy',
    invocation: '/schedule-recurring-agent',
    argument: "<the recurring job> [schedule — e.g. 'weekday 9am' or 'hourly']",
    outcome: 'Creates repeat-safe standing work with an idempotent brief, justified cadence, per-run bound, and off-switch.',
    useWhen: [
      'Triage, monitoring, audits, or digests recur on a real cadence.',
      'A fresh memory-less agent must perform the same bounded job safely each run.',
      'The job needs a durable routine or a deliberately session-scoped cron.',
    ],
    avoidWhen: [
      'The work happens once now.',
      'A current run only needs to wait once for an external signal; use watch-and-act.',
      'The brief cannot deduplicate prior work or define teardown.',
    ],
    workflow: [
      'Confirm the work is recurring and its autonomous action scope is authorized.',
      'Write a self-contained brief with deduplication, reporting, escalation, and a per-run cap.',
      'Choose the least-frequent cadence that still meets the work’s arrival window.',
      'Choose durable cloud routine or in-session cron honestly and confirm registration.',
      'Inspect for duplicates and tear the routine down when its stop condition is met.',
    ],
    guarantees: [
      'Every run starts memory-less but receives a complete, idempotent brief.',
      'The schedule bounds frequency and budget-guard bounds each invocation.',
      'Cadence follows the real arrival rate instead of maximum frequency.',
      'Irreversible and externally visible actions require their own authorization.',
      'Every routine has a teardown condition and reports even an honest nothing-to-do result.',
    ],
    outputs: [
      'Registered routine or session-scoped cron',
      'Self-contained idempotent job brief',
      'Cadence rationale and per-run budget',
      'Reporting, escalation, deduplication, and teardown contract',
    ],
    enforcementArtifacts: [],
    failureStates: [
      'duplicate — an overlapping schedule already exists',
      'not_idempotent — the brief would repeat or spam prior work',
      'unbounded — no per-run cap exists',
      'wrong_mechanism — the selected scheduler cannot meet the required durability',
    ],
    related: ['watch-and-act', 'budget-guard', 'converge-loop'],
    visual: {
      kind: 'cadence-dial',
      title: 'Frequency outside. Bound inside.',
      labels: ['BRIEF', 'DEDUP', 'CADENCE', 'RUN CAP', 'TEARDOWN'],
      caption: 'The schedule decides when to wake; the brief decides how to act safely and when to stop forever.',
    },
  },
  {
    slug: 'auto-map',
    name: 'auto-map',
    title: 'Auto Map',
    version: 'v0.1.0',
    group: 'context',
    invocation: '/auto-map',
    argument: "[scope — full refresh (default), a package/dir, or 'verify' to audit the existing map]",
    outcome: 'Builds a verified, disclosure-tiered context architecture that loads repository knowledge only when needed.',
    useWhen: [
      'A project lacks a trustworthy context map.',
      'Architecture-level changes moved packages, commands, or boundaries.',
      'An existing map needs a drift audit or the pipeline has just shipped meaningful work.',
    ],
    avoidWhen: [
      'A routine edit did not change the project architecture.',
      'The goal is to invent coding policy rather than map observed reality.',
      'The user did not request this durable project-memory mutation and no invoked workflow composes it.',
    ],
    workflow: [
      'Detect project shape and inventory existing context files and ownership.',
      'Discover real directories, commands, boundaries, and conventions.',
      'Write the smallest always-on root, path-scoped rules, nested maps, and linked deep references.',
      'Run the anti-lie validator and execute claimed commands.',
      'Report budgets, preserved human content, drift, and unverified claims.',
    ],
    guarantees: [
      'Paths, exports, routes, and commands are checked against the repository.',
      'Root CLAUDE.md stays at or below 150 lines, rules at 120, and nested maps at 100.',
      'Deep references are linked for on-demand reading rather than imported at startup.',
      'Generated sections update without clobbering human-written instructions.',
      'Commands that cannot run are labeled unverified instead of asserted true.',
    ],
    outputs: [
      'Lean root CLAUDE.md',
      'Path-scoped .claude/rules/*.md files',
      'Nested CLAUDE.md files for significant directories',
      'Verified command and drift report with per-tier line counts',
    ],
    enforcementArtifacts: [
      {
        type: 'validator',
        path: 'auto-map/scripts/verify-map.mjs',
        label: 'Checks generated boundaries, paths, budgets, imports, and map integrity.',
      },
      {
        type: 'templates',
        path: 'auto-map/references/map-templates.md',
        label: 'Defines root, rules, nested-map, stamp, and preservation shapes.',
      },
    ],
    failureStates: [
      'drift — a mapped fact no longer matches repository reality',
      'unverified — a claimed command could not be executed',
      'over_budget — content belongs in a deeper disclosure tier',
      'unsafe_clobber — existing human-owned content cannot be preserved confidently',
    ],
    related: ['auto-learn', 'autonomous-pipeline', 'fan-out-work'],
    visual: {
      kind: 'context-pyramid',
      title: 'Load the minimum. Reveal the rest on demand.',
      labels: ['ROOT ≤150', 'RULES ≤120', 'NESTED ≤100', 'DEEP LINKS'],
      caption: 'Every tier is smaller, later, and verified against the code it describes.',
    },
  },
  {
    slug: 'auto-learn',
    name: 'auto-learn',
    title: 'Auto Learn',
    version: 'v0.1.0',
    group: 'context',
    invocation: '/auto-learn',
    argument: '[checkpoint path or run id to harvest] (default: the most recent .ulpi/runs/*.json)',
    outcome: 'Turns structured run evidence into verified, deduplicated lessons that load automatically next time.',
    useWhen: [
      'An autonomous run has completed and produced checkpoint evidence.',
      'Blocks, guard refusals, thrash, or delegation gaps should change a future decision.',
      'The full pipeline is closing and needs its feed-forward learning pass.',
    ],
    avoidWhen: [
      'The run is still in progress and the evidence is incomplete.',
      'The candidate merely repeats existing repository documentation.',
      'The source is an intuition, secret, one-off flake, or unsupported anecdote.',
    ],
    workflow: [
      'Harvest candidates from checkpoints, open items, guard blocks, fix counts, and degradation records.',
      'Adversarially verify that each candidate is true, general, and actionable.',
      'Deduplicate, merge, prune superseded guidance, and cap additions at five.',
      'Route project, area, and environment lessons into native auto-loaded context.',
      'Surface machine defects to the user and leave the next run a smaller rediscovery burden.',
    ],
    guarantees: [
      'Every candidate carries structured evidence from a completed run.',
      'False, one-off, or non-actionable candidates are rejected before writing.',
      'Lessons land in CLAUDE.md, path-scoped rules, or Claude auto memory—not an inert bespoke file.',
      'Human-written memory is preserved and secrets are never learned.',
      'Memory remains curated, deduplicated, budgeted, and capped to five additions per run.',
    ],
    outputs: [
      'Harvested candidate and evidence ledger',
      'Verified survivors and rejection reasons',
      'Merged lessons in native auto-loaded context',
      'User-facing machine-defect report',
    ],
    enforcementArtifacts: [
      {
        type: 'harvester',
        path: 'auto-learn/scripts/harvest-run.mjs',
        label: 'Extracts evidence-backed candidate lessons from durable run artifacts.',
      },
    ],
    failureStates: [
      'rejected — the candidate is false, too specific, or not actionable',
      'duplicate — an existing memory already captures the lesson',
      'machine_defect — the fix belongs in a skill, guard, or template and is surfaced to the user',
      'unsafe_content — the candidate contains secrets or would overwrite human-owned memory',
    ],
    related: ['auto-map', 'autonomous-pipeline', 'adversarial-verify'],
    visual: {
      kind: 'feed-forward',
      title: 'The next run should not pay twice.',
      labels: ['RUN ARTIFACTS', 'HARVEST', 'VERIFY', 'ROUTE', 'NEXT SESSION'],
      caption: 'Only evidence-backed lessons cross the anti-poison gate into context the agent already loads.',
    },
  },
];

// These fields are common to every skill and are derived from the canonical
// directory name so they cannot drift independently from the slug.
for (const skill of skills) {
  skill.summary = skill.outcome;
  skill.sourcePath = `${skill.slug}/SKILL.md`;
  skill.platforms = ['claude-code', 'codex'];
}

export const plugins = [
  {
    slug: 'claude-code',
    name: 'Claude Code plugin',
    title: 'Claude Code plugin',
    platform: 'Claude Code',
    status: 'current',
    statusLabel: 'Available now',
    shipped: true,
    headline: 'The complete skill pack, plus plugin-level lifecycle hooks and Claude-native orchestration.',
    summary: 'Install all 18 skills as slash commands, with SessionStart, PreToolUse, Stop, and SessionEnd hooks that reinforce resume, live guards, honest termination, and checkpoint cleanup.',
    install: [
      '/plugin marketplace add ulpi-io/skills-autonomous-engineering',
      '/plugin install autonomous-engineering@ulpi-autonomous-engineering',
    ],
    installCommands: [
      '/plugin marketplace add ulpi-io/skills-autonomous-engineering',
      '/plugin install autonomous-engineering@ulpi-autonomous-engineering',
    ],
    capabilities: [
      'All 18 skills as /skill-name commands',
      'Skill-scoped deterministic guard hooks',
      'Plugin lifecycle hooks for resume, live runs, honest stops, and checkpoint GC',
      'Native Workflow templates and goal/loop routing where available',
      'CLAUDE.md, path-scoped rules, and Claude auto-memory integration',
    ],
    caveats: [
      'Irreversible and ambiguous actions still require explicit human authorization.',
      'Static guards block known command spellings; semantic test quality still relies on mutation-check verification.',
    ],
    limitations: [
      'Irreversible and ambiguous actions still require explicit human authorization.',
      'Static guards block known command spellings; semantic test quality still relies on mutation-check verification.',
    ],
    cta: 'Install the Claude Code plugin',
  },
  {
    slug: 'codex',
    name: 'Codex plugin',
    title: 'Codex plugin',
    platform: 'Codex',
    status: 'available',
    statusLabel: 'Available now',
    shipped: true,
    headline: 'A Codex-native control plane for the complete bounded engineering lifecycle.',
    summary: 'The Codex plugin ships all 18 skills as isolated, plugin-qualified adapters, backed by deterministic coordination, pinned codex exec contracts, durable checkpoints, new-session discovery, and reproducible install verification.',
    install: [
      'node scripts/package-codex-plugin.mjs --out /tmp/ulpi-codex-market',
      'codex plugin marketplace add /tmp/ulpi-codex-market',
      'codex plugin add autonomous-engineering@autonomous-engineering',
    ],
    installCommands: [
      'node scripts/package-codex-plugin.mjs --out /tmp/ulpi-codex-market',
      'codex plugin marketplace add /tmp/ulpi-codex-market',
      'codex plugin marketplace list',
      'codex plugin add autonomous-engineering@autonomous-engineering',
      'codex plugin list',
    ],
    capabilities: [
      'All 18 skills as $autonomous-engineering:<skill> adapters',
      'Deterministic coordinator and pinned codex exec executor',
      'Durable checkpoints, bounded fan-out, worktrees, and typed blocked outcomes',
      'Sealed source topology and plugin-qualified new-session discovery',
      'Reproducible marketplace artifact plus isolated add/list and new-session verification',
    ],
    caveats: [
      'Build the current v0.1.0 marketplace artifact from a source checkout before adding it to Codex.',
      'Start a new Codex session after installation; running sessions do not discover newly added skills.',
      'The current source-built artifact does not bundle the repository-level Codex hook wiring.',
      'Surfaces without plugin or hook support return created:false or gateNotRun instead of simulating success.',
    ],
    limitations: [
      'Build the current v0.1.0 marketplace artifact from a source checkout before adding it to Codex.',
      'Start a new Codex session after installation; running sessions do not discover newly added skills.',
      'The current source-built artifact does not bundle the repository-level Codex hook wiring.',
      'Codex does not supply a native scheduler; unsupported creation remains an explicit created:false outcome.',
    ],
    cta: 'Install the Codex plugin',
  },
];

export const skillBySlug = new Map(skills.map((skill) => [skill.slug, skill]));
export const groupBySlug = new Map(groups.map((group) => [group.slug, group]));
export const pluginBySlug = new Map(plugins.map((plugin) => [plugin.slug, plugin]));

export default { groups, skills, plugins };
