# Local example material — reference only, never published with the skills
examples/

# Local working notes (decision log stays between us, not published)
docs/DECISIONS.md

# OS / editor cruft
.DS_Store

# Runtime state that autonomous skills produce in a *target* repo (never committed here)
.ulpi/
docs/REVIEW-61.json
